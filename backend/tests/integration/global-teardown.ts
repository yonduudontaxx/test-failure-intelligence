export default async function globalTeardown(): Promise<void> {
  // Skeleton — populated in Task 7 with `await pool.end()` on the shared
  // test pool when the first repository integration test lands.
}
