export default async function globalSetup(): Promise<void> {
  // Skeleton — populated in Task 7 with the shared test pool and migrations
  // runner when the first repository integration test lands.
}
